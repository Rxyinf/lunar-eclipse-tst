# Stripe (optional for local demo). Without these, a labeled TEST unlock is shown.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Optional. Checkout/portal fall back to the request origin if unset.
NEXT_PUBLIC_APP_URL=http://localhost:3000
